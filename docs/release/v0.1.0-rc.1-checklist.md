# v0.1.0-rc.1 无签名候选验收清单

本文件是长期维护的门禁模板和证据路由表，不复制某一次运行的易失 commit、run ID、文件大小或哈希。最终候选的动态事实记录在 PR #2 的 exact-head 验收证据中，产物字段记录在候选下载包内的 `BUILD-MANIFEST.json` 与 `SHA256SUMS.txt` 中。

## 状态词与门禁类型

- `SEE_PR_EVIDENCE`：从 PR #2 的最终 head 验收记录读取，不在本模板手填。
- `SEE_BUILD_MANIFEST`：从最终候选的构建清单及独立重算记录读取。
- `PENDING`：仅用于仍需人工执行的检查，不表示自动化门禁失败。
- `PASS` / `FAIL`：人工检查实际执行后的结论。
- `NOT_RUN`：明确不在本 RC 范围内，不得解释为通过。
- `AUTO_GATE`：转 Ready 前必须由 exact-head CI 或独立产物复核通过。
- `MANUAL_CHECK`：发布负责人下载后的补充人工验收，不属于 PR 转 Ready 的自动门禁。
- `OUT_OF_SCOPE`：本 RC 明确不实施、不验收。

## 候选身份

| 项目 | 类型 | 证据 |
| --- | --- | --- |
| 候选名称 `0.1.0-rc.1` | `AUTO_GATE` | 本模板与产物文件名 |
| 应用内部版本 `0.1.0` | `AUTO_GATE` | `SEE_BUILD_MANIFEST` |
| 最终 Git commit | `AUTO_GATE` | `SEE_PR_EVIDENCE`、`SEE_BUILD_MANIFEST` |
| Pull Request #2、base `main`、指定 release 分支 | `AUTO_GATE` | `SEE_PR_EVIDENCE` |
| Actions run 与最终 PR head 一致 | `AUTO_GATE` | `SEE_PR_EVIDENCE` |
| 工作流 checkout commit 与候选 commit 一致 | `AUTO_GATE` | `SEE_PR_EVIDENCE`、`SEE_BUILD_MANIFEST` |

## 自动化构建与生命周期门禁

以下项目必须全部在最终 head 的 PR 证据中为 `PASS`；任何新 commit 都使旧结果失效。

| 项目 | 类型 | 证据 |
| --- | --- | --- |
| `desktop-ci` | `AUTO_GATE` | `SEE_PR_EVIDENCE` |
| `macos-arm64-rc` | `AUTO_GATE` | `SEE_PR_EVIDENCE` |
| `rc-manifest` | `AUTO_GATE` | `SEE_PR_EVIDENCE` |
| macOS built-sidecar smoke | `AUTO_GATE` | `SEE_PR_EVIDENCE`、`SEE_BUILD_MANIFEST` |
| macOS packaged-app 正常关闭 smoke | `AUTO_GATE` | `SEE_PR_EVIDENCE`、`SEE_BUILD_MANIFEST` |
| macOS packaged-app 异常退出无 sidecar 残留 | `AUTO_GATE` | `SEE_PR_EVIDENCE` |
| macOS app 与 DMG 内 app 的完整 ad-hoc bundle 签名通过 `codesign --verify --deep --strict` | `AUTO_GATE` | `SEE_PR_EVIDENCE` |
| macOS DMG 只读挂载并检查实际载荷 | `AUTO_GATE` | `SEE_PR_EVIDENCE` |
| READY PID 仅作诊断，停止逻辑不按裸 PID 终止 | `AUTO_GATE` | Rust 测试与 `SEE_PR_EVIDENCE` |
| Unix 专用组清理不影响无关进程 | `AUTO_GATE` | Rust 平台测试与 `SEE_PR_EVIDENCE` |

## 产物与完整性

| 产物 | 大小（字节） | SHA-256 | 类型 | 证据 |
| --- | ---: | --- | --- | --- |
| `qian-labor-desktop-0.1.0-rc.1-macos-arm64-unsigned.app.tar.gz` | `SEE_BUILD_MANIFEST` | `SEE_BUILD_MANIFEST` | `AUTO_GATE` | 构建清单与独立下载重算 |
| `qian-labor-desktop-0.1.0-rc.1-macos-arm64-unsigned.dmg` | `SEE_BUILD_MANIFEST` | `SEE_BUILD_MANIFEST` | `AUTO_GATE` | 构建清单与独立下载重算 |

| 检查 | 类型 | 证据 |
| --- | --- | --- |
| `SHA256SUMS.txt` 与独立重算值一致 | `AUTO_GATE` | `SEE_PR_EVIDENCE`、`SEE_BUILD_MANIFEST` |
| `BUILD-MANIFEST.json` 必填字段完整 | `AUTO_GATE` | `SEE_PR_EVIDENCE`、`SEE_BUILD_MANIFEST` |
| 清单 commit 等于最终候选 commit | `AUTO_GATE` | `SEE_PR_EVIDENCE`、`SEE_BUILD_MANIFEST` |
| 清单大小、SHA-256 等于独立下载文件 | `AUTO_GATE` | `SEE_PR_EVIDENCE`、`SEE_BUILD_MANIFEST` |
| 文件名全部为 ASCII 且严格等于上表 | `AUTO_GATE` | `SEE_PR_EVIDENCE`、`SEE_BUILD_MANIFEST` |
| GitHub Actions artifact 留存期为 14 天 | `AUTO_GATE` | workflow 配置与 `SEE_PR_EVIDENCE` |

## 安全与范围门禁

| 项目 | 类型 | 证据/状态 |
| --- | --- | --- |
| 清单 `signed=false` | `AUTO_GATE` | `SEE_BUILD_MANIFEST` |
| 清单 `notarized=false` | `AUTO_GATE` | `SEE_BUILD_MANIFEST` |
| 仅使用 synthetic 测试材料 | `AUTO_GATE` | `SEE_PR_EVIDENCE` |
| 安装包无 `.env`、SQLite、日志、缓存、fixture 或测试输出 | `AUTO_GATE` | `SEE_PR_EVIDENCE` |
| 安装包无真实 Key、token、个人数据和构建机绝对路径 | `AUTO_GATE` | `SEE_PR_EVIDENCE` |
| sidecar 仅绑定 loopback，业务 API 与 shutdown API 均受 token 鉴权 | `AUTO_GATE` | `SEE_PR_EVIDENCE` |
| 未创建 Git tag | `AUTO_GATE` | `SEE_PR_EVIDENCE` |
| 未创建 GitHub Release | `AUTO_GATE` | `SEE_PR_EVIDENCE` |
| Developer ID 签名 | `OUT_OF_SCOPE` | `NOT_RUN` |
| macOS 公证 | `OUT_OF_SCOPE` | `NOT_RUN` |
| 智谱真实 Provider + synthetic 材料 smoke | `AUTO_GATE` | `SEE_PR_EVIDENCE`；不把 Key 注入 GitHub Actions |
| 图片/VLM smoke | `OUT_OF_SCOPE` | `NOT_RUN` |
| macOS Keychain 中的 Key/pepper 不进入 React、SQLite、日志或安装包 | `AUTO_GATE` | Rust 测试、敏感扫描与 `SEE_PR_EVIDENCE` |
| 自动更新 | `OUT_OF_SCOPE` | `NOT_RUN` |

## Ready 判定

PR #2 只有在以下条件同时满足时才能从 Draft 转为 Ready：最终 PR head 的 `desktop-ci`、`macos-arm64-rc` 与 `rc-manifest` 全部通过；macOS built-sidecar、正常 packaged-app、Launch Services 启动和异常退出清理门禁全部通过；两项最终下载产物的大小与 SHA-256 经独立重算并与清单一致；使用用户自有 Key 的真实 Provider synthetic 验收通过；安全检查无失败；review conversation 无未解决阻断项。

任一自动化门禁为失败、取消、跳过或证据仍未形成时，PR 必须保持 Draft。转换 Ready 仅表示候选已达到代码审查门槛，不授权合并、打 tag、发布 Release、签名或公证。

## 补充人工安装与功能验收

本节为发布负责人下载后的 `MANUAL_CHECK`，不由仓库模板预填为通过，也不替代上面的 exact-head 自动化门禁。先使用完全虚构的 synthetic 材料；真实 Provider 验收只在应用设置页输入用户自己的 Key，不在聊天、日志、fixture 或 CI 中传递 Key，也不要全局关闭 Gatekeeper。

- 安装包 SHA-256 已在安装前人工核对：`PENDING`
- macOS `.dmg` 可挂载并将 `.app` 复制到测试位置：`PENDING`
- 首次启动可进入智谱设置页：`PENDING`
- Key 可通过连接测试，重启后仍只显示“已配置”而不回显：`PENDING`
- synthetic 材料可导入：`PENDING`
- Dashboard 可显示分析状态和风险结果：`PENDING`
- 人工员工匹配完成后可继续规则计算：`PENDING`
- 员工台账、员工详情和报告可打开：`PENDING`
- 系统打印可选择“存储为 PDF”：`PENDING`
- 风险详情可显示来源追溯：`PENDING`
- 退出并重新启动后分析记录仍持久化：`PENDING`
- 删除分析后 Dashboard 与风险详情不可再访问：`PENDING`
- 退出主程序后 sidecar 不残留：`PENDING`
- macOS 测试 `.app` 和挂载卷已清理：`PENDING`
- 当前用户测试数据目录已按需清理：`PENDING`

| 平台 | 安装 | 首次启动 | synthetic 导入 | Dashboard | 来源追溯 | 重启持久化 | 删除清理 | sidecar 清理 | 卸载/移除 | 结论 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| macOS arm64 | `PENDING` | `PENDING` | `PENDING` | `PENDING` | `PENDING` | `PENDING` | `PENDING` | `PENDING` | `PENDING` | `PENDING` |

## 已知限制

- 候选不包含 macOS Developer ID 签名或公证。
- macOS 候选仅使用 ad-hoc 签名，并明确关闭 hardened runtime，以兼容内置的 PyInstaller sidecar；正式分发签名与公证仍不在本 RC 范围。
- Windows 安装包、多 Provider、Word 报告、图片/VLM smoke 和自动更新不在本 RC 范围。
- 工作流锁定工具链和依赖解析，但不声明安装包逐位可复现。
- 真实 Provider 验收必须由用户在应用内输入自己的 Key；缺少该验收证据时 PR 保持 Draft。

## 剩余自动化阻断项

从 PR #2 的最终 head 验收证据读取：`SEE_PR_EVIDENCE`。
