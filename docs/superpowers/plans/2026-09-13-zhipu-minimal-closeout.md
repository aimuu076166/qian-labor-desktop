# 智谱分析最小修复收口

用户批准：保留 GLM 基于 2d0dfdf 的补丁，补齐审核发现的四组缺口。停止发送前材料脱敏；继续保护密钥、人工复核和本地来源核验。不新增打印、OCR/PDF、工资考勤核算或架构功能。

1. Provider 逐条归一化、严格校验、转换；19 条有效事实不能被一条异常清空。异常保留安全原因及原始索引；只在整个响应无法读取时考虑一次格式修复。
2. 未接收项使用已有审计元数据持久化，工作区展示数量及原因，部分完成可显式重试；成功缓存与历史不被清空。
3. 试用期统一使用现有日期对数组，并校验真实日期和先后顺序；无法转换的保留复核状态。JSON/list 冲突不得回退成不相干文本。
4. 修复请求最多一次；发出前检查取消；累计两次用量；HTTP/超时错误不能被第一轮格式错误遮盖。

先写并运行失败测试，再实现。复验 Provider、数据库持久化/缓存、前端、完整回归和构建。随后用原来失败的五份合同进行正常 App 验收；不以模拟测试替代真实验收，不将真实材料或响应提交到 Git。最终包仍交付原 GitHub 仓库，不合并 PR。

基线审核：Python 916 passed / 7 failed，七项在同环境未改基线同样失败；前端 281 passed，lint/typecheck 通过。失败涉及本机 sidecar 启动、原生 OCR 环境和 cargo PATH，非本轮新回归。处理交付环境时单独验证，不修改业务测试绕过失败。


## 本轮执行记录

已完成四项修复，并通过独立规格与代码质量复核。另修正了停止脱敏时误删的本地 citation ID 排除规则：只排除本地身份哈希扫描，发送正文仍是原文。

验证：完整 Python 回归 951 passed；随后新增精度保护测试及测试导入路径修正，相关 42 项复验通过。前端 284 passed，lint/typecheck/build 通过；Rust 29 passed；敏感信息扫描、macOS 深度签名校验、打包 App 合成流程及异常退出清理通过。未经授权不扩展打印/OCR 功能。

最初的真实验收因 Mac 锁定暂停。用户解锁，并明确授权将当前材料库五份合同原文发送至已配置的智谱官方 Coding Plan 后，已执行正常 App 验收（不是直接脚本替代）。没有读取密钥，没有保存或提交真实材料、原始响应。

## 2026-09-13 实际验收与补充修复

实际请求定位到逐条事实内的扩展字段仍被 strict schema 整条拒收。现在仅忽略事实层的扩展字段，声明值载体仍严格校验；数值 1 不能冒充布尔 true。声明了类型但所有值载体均为空的事实保留为未知、需人工复核，不推断结束日期。另补了有界字段级安全诊断、元数据修复提示和仅两个完全相同 JSON 对象的重复响应兼容；矛盾对象仍拒绝。

最终三个待重试文件均一次正常请求成功，最新未接收项为 0；另外两份成功文件复用缓存。五份材料均 processed，分别保留 14、14、15、12、12 条事实，共 67 条（包含先前尝试保留的历史，不声称本轮新增 67 条）。合同条款观察五份均执行；没有用自动化 Fake 结果冒充真实智谱结果。

按正文核对姓名后，经正常界面确认五份材料员工归属。发现旧代码将手动填写的短显示名全部变成 ***，补了最小本地更正入口：保留手工显示名，版本冲突拒绝覆盖，只同步当前快照，不改事实、匹配归属、历史快照或冻结报告。通过该界面恢复五名员工显示名，不直接改数据库，不重新调用模型。此更正仅使用现有字段，没有新增表或数据迁移。

实测员工页、事实核对、合同条款观察和报告版本均可打开。更正姓名后报告版本 2 保存成功，版本 1 哈希保持不变。风险页为 5 名员工、72 条风险或资料事项；并非 72 项已确认风险。仍有缺失材料、来源未定位和人工复核事项，不能宣称所有来源或所有风险判断均已核实；打印、OCR/PDF 和工资考勤核算继续不在本轮交付验收范围。

最终源码：Python 964 passed，前端 286 passed，lint/typecheck 通过；Rust 29 passed。侧载服务合成全链路与深度签名校验通过。真实模型调用确认发生在智谱官方 Coding Plan / glm-5.3-flash，后续显示名更正及报告保存没有增加调用数。最终 GitHub 安装包尚未生成及独立验收，PR #2 保持 Draft、不合并；源码推送和 CI 产物属于下一道门禁，不得拿旧 head 安装包替代。
